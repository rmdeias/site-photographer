<?php ob_start() ?>

    <form action="Models/fashion/gestionCategories/insertUpdate.php" method="Post" enctype="multipart/form-data">
        <fieldset>
            <legend>Fashion: Gestion Catégorie</legend>
            <ul>
                <li>
                    <label>Titre catégorie:</label>
                    <input type="text" name="categorie"
                           id="categorie" <?php if (isset($listeCategorie)): ?> value="<?= $listeCategorie["categorie"] ?>"<?php endif; ?>
                           required>
                </li>

                <li>
                    <label>Photo:</label>
                    <input type="file" name="photoCouv"
                           id="photoCouv" <?php if (!isset($listeCategorie["categorie"])): ?> required <?php endif; ?>>
                </li>

                <li>
                    <?php if (isset($listeCategorie)): ?>
                        <input type="hidden" name="id" id="id" value="<?= $listeCategorie["id"] ?>">
                    <?php endif; ?>
                </li>

            </ul>
            <button type="submit">Enregistrer</button>
        </fieldset>
    </form>

<?php $content = ob_get_clean(); ?>